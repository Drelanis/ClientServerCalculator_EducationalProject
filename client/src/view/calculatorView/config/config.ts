enum tags {
  div = 'div',
  span = 'span',
  i = 'i',
  button = 'button',
  label = 'label',
  input = 'input',
  textarea = 'textarea',
}

enum atributesNames {
  for = 'for',
  id = 'id',
  type = 'type',
  placeholder = 'placeholder',
}

enum atributesValues {
  equal = 'equal',
  checkbox = 'checkbox',
  text = 'text',
  inputPlaceholder = 'Expression input',
}

enum eventsType {
  click = 'click',
  input = 'input',
  change = 'change',
  mouseDown = 'mousedown',
  keyDown = 'keydown',
  keyPress = 'keypress',
}

export { tags, atributesNames, atributesValues, eventsType };
